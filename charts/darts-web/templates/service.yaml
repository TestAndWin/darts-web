apiVersion: v1
kind: Service
metadata:
  name: {{ include "darts-web.fullname" . }}
  labels:
    {{- include "darts-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "darts-web.selectorLabels" . | nindent 4 }}
